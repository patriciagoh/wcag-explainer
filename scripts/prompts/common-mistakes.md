# Common Mistakes

List 3–5 specific, concrete mistakes engineers make that violate this WCAG criterion. Lead with the most common. Reference real patterns (e.g., "using `<div onClick>` instead of `<button>`"), not abstract failures.

## Criterion

**{{id}} {{title}}** (Level {{level}})

{{officialText}}

## Axe rules that flag this criterion

{{axeRulesDescriptions}}

## Output format

A JSON array of strings, 3–5 items. Each item is one mistake, one sentence. Example:
```
[
  "Using a div with onClick instead of a real button element",
  "..."
]
```
